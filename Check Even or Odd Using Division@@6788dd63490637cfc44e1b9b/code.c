#include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    int q;
    q = (x/2);
    if(q*2==x){
        printf("Even");
    }
    else{
        printf(odd);
    }
    return 0;
}