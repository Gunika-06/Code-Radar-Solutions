#include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    int quotient;
    quotient = x/2;
    if(quotient*2==x){
        printf("Even");
    }
    else{
        printf(odd);
    }
    return 0;
}